// View types — the only type imported from this barrel.
// All other types (Agent, Task, etc.) are imported directly from their stores.
export type ViewMode = 'chat' | 'dashboard' | 'docs' | 'sandbox'
